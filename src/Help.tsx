import React from 'react';

interface HelpProps {
    // Define your props here, for example:
    // theme: 'light' | 'dark';
}

const Help: React.FC<HelpProps> = () => {

    return (
        <div>
            <h1>Help</h1>
        </div>
    );
}

export default Help;